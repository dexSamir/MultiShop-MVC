@{
    ViewBag.Title = "Create Category";
}
@model CategoryCreateVM
<div class="container text-center">
    <div class="row">
        <div class="col">
            <div class="card shadow">
                <div class="card-header mb-3 d-flex justify-content-between align-items-center">
                    <h6 class="font-weight-bold text-primary">Create Category</h6>
                    <a asp-action="Index" asp-controller="Category" class="btn btn-outline-primary">Return List</a>
                </div>
                <div class="card-body">
                    <form method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label asp-for="@Model.Name" class="form-label"></label>
                            <input asp-for="@Model.Name" class="form-control" aria-describedby="emailHelp">
                            <span asp-validation-for="@Model.Name" class="form-text text-small text-danger"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="@Model.Image" class="form-label"></label>
                            <input asp-for="@Model.Image" accept="image" class="form-control">
                            <span asp-validation-for="@Model.Image" class="form-text text-small text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-primary">Submit</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>